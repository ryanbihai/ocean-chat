# OceanBus Chat configuration
# Copy this file to config.yaml and edit as needed

# OceanBus L0 API endpoint (default is the public test server)
# oceanbus_base_url: https://ai-t.ihaola.com.cn/api/l0

# Yellow Pages service OpenID (required — contact admin to obtain)
# Environment variable: OCEANBUS_YP_OPENIDS
# oceanbus_yp_openids: "xxxxxxxxxxxx"
