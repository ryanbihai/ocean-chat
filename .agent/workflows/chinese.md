---
description: 强制中文
---

所有的思考过程，建议，计划，都用中文
全局中文原则：除代码关键字（如变量名、函数名、库名）外，所有输出强制使用中文（简体）。
覆盖范围：
思考链 (Thought Process)：所有内部推理和决策过程。
任务管理 (Task Management)：task_boundary 工具中的 TaskName（任务名称）、TaskStatus（状态描述）和 TaskSummary（总结）必须用中文。
文档工件 (Artifacts)：
task.md、implementation_plan.md等文档的标题、列表项和说明文字。