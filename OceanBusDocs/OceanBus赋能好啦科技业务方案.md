# OceanBus 赋能好啦科技业务方案

> AI Agent 时代的保险健康服务基础设施 —— 让保司、代理人、C端客户彼此发现、信任、交易。

---

## 一、既有业务基础

### 1.1 好啦科技定位

北京好啦科技有限公司成立于 2015 年，专注**保险业健康测评与体检服务**，深耕 11 年。

| 核心指标 | 数据 |
|---------|------|
| 合作保险公司 | 70+，覆盖中国保险业 55%+ 市场 |
| 累计健康测评用户 | 3400 万+ |
| 合作体检机构 | 800+，覆盖 30 省 200+ 城市 |
| 大客户续约率 | 100%（2018 年起） |
| 客户满意度 / 投诉率 | 99% / 0% |

**四大核心能力**：测评获客引擎 → "核力"智能核保平台 → VIP 体检服务体系 → AI Agent 双引擎（ocean-agent + AI 健康管家）。

### 1.2 Bot2.0 — "好啦管家"

**定位**：嵌入保司 App/H5 的 AI 健康管家聊天机器人，帮 C 端客户做健康测评、体检套餐定制、机构/医生查询。

**架构**：Express 服务端 + Vue 聊天前端 + ReAct Agent 引擎 + Skill 技能体系。

```mermaid
flowchart LR
    A[C端用户<br>打开保司App] --> B[点击 AI健康管家]
    B --> C[聊天界面]
    C --> D["Bot2.0 服务端<br>────────<br>意图识别 → Skill路由<br>→ LLM推理 → API调用"]
    D --> E[返回结果<br>套餐推荐 + 机构推荐 + 价格]
```

**当前 Skill 能力**：

| Skill | 功能 |
|-------|------|
| `custom-package-generator` | 体检套餐定制 + 推荐理由 |
| `institution-query` | 机构查询（800+ 可查） |
| `doctor-query` | 医生查询 |
| `underwriting` | 核保辅助 |
| `rewrite-profile` | 健康档案更新 |
| `ask-user-question` | 专业追问澄清 |

### 1.3 Bot2.0 的关键局限

```mermaid
flowchart TD
    subgraph 现状
        A1[用户必须打开保司App才能触达] 
        A2[被动等待提问<br>不能主动发现服务]
        A3[随session生灭<br>无持久化数字身份]
        A4[没法告诉用户<br>服务机构靠不靠谱]
        A5[不能和代理人/机构<br>的AI通信——信息孤岛]
    end
    
    A1 -.->|可达性缺失| X((信息孤岛))
    A2 -.->|主动性缺失| X
    A3 -.->|身份缺失| X
    A4 -.->|信任缺失| X
    A5 -.->|互联缺失| X
    
    style X fill:#C8963E,stroke:#8B6914,color:#fff
```

---

## 二、OceanBus 赋能：三角色 × 三产品

### 2.1 整体架构

```mermaid
flowchart TB
    subgraph 产品层["产品交付层"]
        direction LR
        P1["🔵 bot2.0 升级版<br>────────<br>C端用户<br>AI健康管家"]
        P2["🟡 保司驾驶舱<br>────────<br>保司管理员<br>渠道运营中心"]
        P3["🟢 ocean-agent<br>────────<br>代理人<br>24h获客工作台"]
        P4["🟠 机构通<br>────────<br>机构运营<br>服务管理后台"]
    end

    subgraph 中间层["好啦中间层"]
        M1[用户管理]
        M2[体检系统对接]
        M3[测评引擎]
        M4[核保引擎]
        M5[套餐推荐]
        M6[福利匹配]
        M7[OceanBus Agent 管理]
    end

    subgraph 协议层["OceanBus 协议层"]
        direction LR
        O1[L0 加密消息路由]
        O2[L1 黄页服务]
        O3[L1 声誉服务]
    end

    P1 --> M1
    P1 --> M3
    P1 --> M5
    P2 --> M1
    P2 --> M7
    P3 --> M7
    P3 --> M2
    P4 --> M2
    P4 --> M7

    M7 --> O1
    M7 --> O2
    M7 --> O3
```

### 2.2 角色一：C端客户 — bot2.0 升级版

**现状**：一个嵌入保司 App 的聊天窗口，等用户提问。

**升级后**：用户的"好啦管家"升级为**持久化的个人 AI 健康助手，拥有 OceanBus Agent 身份**。当识别到用户存在可保风险时，管家 Agent 主动搜索黄页——不是搜人类，是搜**代理人的 AI 替身（ocean-agent）**。两个 Agent 先完成信息收集、需求匹配、意向确认，确认值得见面后，才通知人类代理人和用户。

**核心体验变化**：

```mermaid
flowchart TD
    subgraph 过去["过去：被动问答"]
        U1[用户打开保司App]
        U2[输入问题]
        U3[返回固定结果]
        U1 --> U2 --> U3
    end

    subgraph 现在["现在：主动发现 + 信任决策"]
        V1[用户日常使用<br>手机/微信/AI助手]
        V2["管家Agent拥有<br>OceanBus持久身份"]
        V3["搜索黄页发现服务<br>discover({tags})"]
        V4["查询声誉判断可信度<br>queryReputation([openid])"]
        V5["Agent间加密通信<br>send(openid, msg)"]
        V6["推送结果给用户<br>附信任信号"]
        V7[用户确认/决策]
        V1 --> V2
        V2 --> V3
        V3 --> V4
        V4 --> V5
        V5 --> V6
        V6 --> V7
    end

    style 过去 fill:#f0f0f0,stroke:#999
    style 现在 fill:#EDF4FF,stroke:#3E89FB
```

**典型场景链路**：

```mermaid
sequenceDiagram
    actor 用户
    participant 管家 as 管家Agent<br>(bot2.0升级版)
    participant 黄页 as OceanBus 黄页
    participant 声誉 as OceanBus 声誉服务
    participant 机构 as 体检机构Agent
    participant 代理人AI as 代理人AI替身<br>(ocean-agent)
    participant 代理人 as 人类代理人

    Note over 用户,管家: 场景一：找体检机构

    用户->>管家: "帮我找附近能做胃肠镜的"
    管家->>黄页: discover({tags:["体检","胃肠镜","北京"]})
    黄页-->>管家: 返回3家机构 + OpenID
    管家->>声誉: queryReputation([openid1, openid2, openid3])
    声誉-->>管家: 标签分布 + 标记者画像 + 通信拓扑
    管家->>管家: AI综合分析<br>"朝阳分院最靠谱，可靠8900，心跳5分钟前"
    管家-->>用户: 推荐结果 + 信任信号
    用户->>管家: "就这家"
    管家->>机构: send(openid, "预约12月20日胃肠镜")
    机构-->>管家: "已确认，12月20日9:00"

    Note over 用户,管家: 场景二：体检发现风险 → 搜索代理人的AI替身

    管家->>管家: "报告显示甲状腺结节3级<br>建议咨询重疾险顾问"
    管家->>黄页: discover({tags:["保险","甲状腺","重疾"]})
    黄页-->>管家: 返回5位代理人的AI替身<br>(每条黄页条目背后是 ocean-agent)
    管家->>声誉: queryReputation(5个openids)
    声誉-->>管家: 标签画像对比

    Note over 管家,代理人AI: ★ Agent 与 Agent 先对话，人类不参与 ★

    管家->>代理人AI: send("36岁男，甲状腺结节3级，<br>想了解重疾险投保条件。<br>担心结节影响核保，预算5-8k。")
    代理人AI-->>管家: "这是张顾问最熟悉的领域。<br>她处理过200+类似案例。<br>TI-RADS 3类多数公司除外承保，<br>少数可标体。具体分级是？"
    管家->>代理人AI: "TI-RADS 3类，没做过穿刺。"
    代理人AI-->>管家: "张顾问手上有3款适合的产品。<br>要不要约时间让张顾问详细讲？"

    Note over 代理人AI,代理人: 确认匹配后，才通知人类代理人

    代理人AI->>代理人: 🔥"有位高意向客户，<br>AI已帮你聊了18轮，<br>约了明天下午2点见面。"

    管家-->>用户: "已帮你找到张顾问，<br>从业8年，238位客户评价可靠，<br>明天下午2点见面。"
```

**交付产品**：bot2.0 现有聊天界面的升级版。用户感知不到 OceanBus——只感知到"这个管家现在能帮我找到更多服务了，还告诉我靠不靠谱"。

---

### 2.3 角色二：代理人 — ocean-agent 小程序

**现状**：发测评海报到朋友圈 → 等客户扫码 → 加微信聊 → 约见面。获客靠运气，转化靠能力。

**升级后**：代理人拥有一个 24 小时在线的**数字替身**，在黄页上被 AI 搜索和发现，Agent 自动承接、筛选、初步沟通，只把高意向客户推到人面前。

**价值转变**：

```mermaid
flowchart LR
    subgraph 过去["过去：冷拜访"]
        A1[发朋友圈海报]
        A2[等待客户扫码]
        A3[加微信寒暄]
        A4[手动跟进]
        A5["转化率：?"]
        A1 --> A2 --> A3 --> A4 --> A5
    end

    subgraph 现在["现在：热咨询"]
        B1["Agent 24h<br>黄页在线"]
        B2["客户AI主动搜索<br>发现代理人"]
        B3["查声誉标签<br>自动建立信任"]
        B4["AI先聊确认意向<br>筛选高价值客户"]
        B5["推送给人<br>确认见面"]
        B1 --> B2 --> B3 --> B4 --> B5
    end

    style 过去 fill:#f0f0f0,stroke:#999
    style 现在 fill:#EDF4FF,stroke:#3E89FB
```

**代理人早起看到的画面**：

```mermaid
flowchart TD
    subgraph 日报["ocean-agent 每日简报"]
        D1["📊 过去 24 小时<br>你的 Agent 被搜索：47 次"]
        D2["🤖 自动承接对话：5 位"]
        D3["🔥 筛选后高意向：3 位"]
        D4["📅 已帮你约见面：2 位"]

        subgraph 待办["📋 今日待处理"]
            T1["🔥 王先生<br>甲状腺重疾 · AI已沟通<br>约明天下午见面<br>[查看对话] [确认]"]
            T2["📝 李女士<br>家庭健康险 · 预算3-5k<br>需你回复确认时间<br>[查看对话] [回复]"]
            T3["👀 赵先生<br>子女教育金 · AI正在沟通<br>[查看实时对话]"]
        end

        E1["📈 我的声誉  可靠标签 238<br>本月被AI发现 340次 | 转人工 23次"]
    end

    D1 --> D2 --> D3 --> D4
    D4 --> 待办
    待办 --> E1
```

**关键变化**：代理人不需要理解 OceanBus、OpenID、sync——他们只需要打开小程序，看"今天谁找我"，点一下确认。注册流程是扫个码、选几个标签、填一句话简介。

---

### 2.4 角色三：保险公司 — 保司驾驶舱

**现状**：不知道客户从哪来、不知道代理人服务好坏、想做 AI 但不知道怎么做。

**升级后**：获得一个完整的**AI Agent 渠道运营中心**——归因有数据、能力可量化、合规可管控、Agent 零自研成本。

```mermaid
flowchart TD
    subgraph 驾驶舱["保司驾驶舱"]
        direction TB
        
        subgraph 总览["📊 渠道总览"]
            S1["活跃代理人 2,341"]
            S2["本月AI搜索 15.6万次"]
            S3["AI转人工 3,200次"]
            S4["约见面 890次"]
            S5["成交 320单"]
        end

        subgraph 归因["📈 渠道归因（OpenID协议级追踪）"]
            C1["黄页自然搜索 ████████████ 52%"]
            C2["健康测评转化 ██████ 24%"]
            C3["银保合作入口 ████ 16%"]
            C4["其他 ██ 8%"]
        end

        subgraph 排行["🏆 代理人声誉排行榜"]
            R1["1. 张三  可靠238  转化率9.2%  重疾专家"]
            R2["2. 李四  可靠156  转化率7.1%  医疗险顾问"]
            R3["3. 王五  可靠98   转化率5.3%  家庭保障"]
        end

        subgraph 配置["⚙️ 配置管理"]
            G1["代理人黄页模板"]
            G2["自动回复策略"]
            G3["合规拦截词库"]
            G4["OpenID渠道分发"]
            G5["配额管理"]
        end
    end

    总览 --> 归因
    归因 --> 排行
    排行 --> 配置
```

**为什么保司愿意买单**：

| 价值 | 过去 | OceanBus 化后 |
|------|------|--------------|
| **代理人能力管理** | 只看保费——结果考核 | 声誉标签 + 转化率 + 社交广度——能力可量化 |
| **获客归因** | 问代理人"客户从哪来"——数据靠汇报 | OpenID 协议级渠道归因——自动统计 |
| **合规管控** | 代理人用个人微信沟通——不可控 | 拦截词过滤 + 加密消息审计——平台级管控 |
| **代理人离职影响** | 人走客户走 | 声誉绑定保司 Agent UUID——声誉资产留在平台 |
| **AI 研发成本** | 需自建 AI 团队 | 驾驶舱开箱即用——零自研 |

---

## 三、交付方式

### 3.1 一次对接，三个产品

```mermaid
flowchart TB
    subgraph 对接["保司对接流程（已有）"]
        Step1[签约]
        Step2[系统对接<br>测评嵌入 + 体检API]
        Step3[上线交付]
        Step1 --> Step2 --> Step3
    end

    subgraph 新增["新增一步（透明完成）"]
        New1["为保司创建 OceanBus<br>品牌Agent + UUID"]
        New2["为代理人池批量<br>注册 ocean-agent"]
        New3["开通保司驾驶舱<br>管理员账号"]
        New4["生成代理人<br>扫码注册邀请"]
        New1 --> New2 --> New3 --> New4
    end

    对接 --> 新增

    subgraph 交付["交付物"]
        D1["bot2.0 升级版<br>嵌入保司App/H5"]
        D2["保司驾驶舱<br>Web后台账号"]
        D3["ocean-agent<br>代理人小程序码"]
    end

    新增 --> 交付
```

### 3.2 各角色学习成本

| 角色 | 看到的产品 | 需要理解 OceanBus？ | 上手操作 |
|------|-----------|-------------------|---------|
| C 端客户 | bot2.0 聊天界面（升级版） | 不需要 | 跟以前一样打字聊天 |
| 代理人 | ocean-agent 小程序 | 不需要 | 扫码 → 选标签 → 填简介 → 完成 |
| 保司管理员 | 保司驾驶舱 Web 后台 | 不需要 | 看数据、配模板、发邀请 |
| 机构运营 | 机构通 Web 后台 | 不需要 | 改简介、看数据、设回复 |

**OceanBus 是协议层，不是交付物。** 所有角色接触的都是他们熟悉的业务界面。

---

## 四、竞争壁垒

| 壁垒 | 来源 | 为什么竞品抄不了 |
|------|------|-----------------|
| **供给侧网络** | 70+ 保司 + 800+ 机构 + 代理人池 | 11 年商务关系积累 |
| **声誉不可逃性** | L0 reverse-lookup 仅官方可调用 | 第三方黄页信誉体系天然漏的 |
| **信任网络效应** | 标签越多 → 黄页越有效 → 越多 Agent 注册 | 双边网络，冷启动成本极高 |
| **数据深度** | 3400 万测评 + 3000+ 风险模型 | AI 搜索匹配精度碾压通用方案 |
| **合规护城河** | 等保三级 + ISO 27001 + 监管关系 | 保险行业天然高合规门槛 |

---

## 五、一句话总结

```
好啦科技用 11 年建成了保险健康领域最大的供给侧网络。
OceanBus 给这张网络装上"发现 → 信任 → 交易"的 AI Agent 引擎。

C端客户的 AI 管家能主动搜到服务、看声誉做决策。
代理人的 AI 替身 24h 被搜索、自动筛选高意向客户。
保司获得完整的 AI Agent 渠道运营中心——归因有数据、能力可量化。

三层角色，三个产品，一次对接，零新概念学习成本。
```
